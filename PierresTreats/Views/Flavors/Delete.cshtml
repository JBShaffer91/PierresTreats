@model PierresTreats.Models.Flavor

<h2>Delete @Model.Name?</h2>

<form asp-action="DeleteConfirmed">
  <input type="hidden" asp-for="FlavorId" />
  <input type="submit" value="Delete" class="btn btn-danger" />
</form>
